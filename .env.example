# Copy this file to .env and fill in your values.
# Without Supabase keys → app runs in demo mode (local mock login).
# Without Gemini key → AI dossier / eligibility features are disabled.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_GEMINI_API_KEY=
